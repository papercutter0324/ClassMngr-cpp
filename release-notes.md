# ClassMngr 0.9.1 Release Notes

ClassMngr 0.9.1 is a maintenance release that improves the reliability and
layout of Excel schedule imports on macOS.

## Schedule Import Fixes

- Fixed a crash that could occur after successfully completing a schedule
  import.
- Fixed the schedule import dialog sizing on macOS so its controls remain
  readable as the dialog content changes.
